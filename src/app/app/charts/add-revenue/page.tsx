import { RevenuePage } from '@/app/Componnets/Report/RevenuePage';

type PageType = {
};

const Page: React.FC<PageType> = ({ }) => {

    return (
        <RevenuePage />
    );
};

export default Page;